rules_version = '2';

// GlobalChat Earn Development & Preview Rules
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
